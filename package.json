{
  "name": "vaultly",
  "version": "1.0.0",
  "description": "Vaultly — Personal expense tracker with PostgreSQL backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "init-db": "node init-db.js",
    "build": "node copy-assets.js",
    "deploy": "node copy-assets.js && wrangler deploy",
    "cf-dev": "node copy-assets.js && wrangler dev"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0",
    "@neondatabase/serverless": "^0.9.4",
    "jose": "^5.6.3"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
